import introVideoPoster from '@/assets/intro-video-poster.png'

/**
 * STUBBED — stands in for the future Strapi `getSiteConfig()` client
 * (unbuilt, see TODO.md Ticket #3). `videoSrc` is a public sample clip,
 * not the real CommPassion intro video — see STUBBED_DATA.md.
 */
export const siteConfig = {
  introVideo: {
    videoSrc: 'https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4',
    posterSrc: introVideoPoster,
  },
}
